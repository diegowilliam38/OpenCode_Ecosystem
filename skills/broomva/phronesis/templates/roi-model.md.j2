{# templates/roi-model.md.j2 — ROI Model (Deliverable 5 of 7).

Year-by-year RoiCells for top-3 use cases with sensitivity bands.
P3 invariant in footer.
#}
# ROI Model — {{ tenant.name }}

**Engagement:** `{{ tenant.tenant_slug }}`
**Date:** {{ generated_at }}
**Discount rate:** {{ "%.1f" | format(discount_rate * 100) }}% (tenant WACC)

## Year-1 net (top-3)

| Use Case | Revenue Impact | Investment | Net | Sensitivity (low/high) |
|---|---|---|---|---|
{% for cell in roi_cells %}
| `{{ cell.use_case_id }}` | {{ cell.revenue_impact | as_currency }} | {{ cell.investment | as_currency }} | {{ cell.net | as_currency }} | {{ cell.sensitivity_low | as_currency }} / {{ cell.sensitivity_high | as_currency }} |
{% endfor %}

## Combined Year-1

| | |
|---|---|
| **Total revenue impact** | {{ total_revenue | as_currency }} |
| **Total investment** | {{ total_investment | as_currency }} |
| **Combined net** | {{ total_net | as_currency }} |

## Assumptions

{% for a in assumptions %}
- {{ a }}
{% endfor %}

---

_Generated by phronesis. P3 ✓ all use cases cite evidence; sensitivity bands surface optimism risk._
