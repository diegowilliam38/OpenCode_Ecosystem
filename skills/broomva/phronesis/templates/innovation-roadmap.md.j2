{# templates/innovation-roadmap.md.j2 — Innovation Roadmap (Deliverable 6 of 7).

RoadmapStep[] grouped by Three-Horizons (h1-now, h2-next, h3-later).
P7 invariant in footer.
#}
# Innovation Roadmap — {{ tenant.name }}

**Engagement:** `{{ tenant.tenant_slug }}`
**Date:** {{ generated_at }}
**Strategic thesis:** {{ thesis.economic_lever }} (horizon: `{{ thesis.strategic_horizon }}`)
**Frameworks applied:** {{ frameworks_applied | join(", ") }}

## Three-Horizons plan

{% for horizon, label in [("h1-now", "H1 — Now (deploy this fiscal year)"),
                          ("h2-next", "H2 — Next (prove this year, scale next)"),
                          ("h3-later", "H3 — Later (real options, may kill)")] %}
### {{ label }}

{% set steps_in_horizon = roadmap_steps | selectattr("horizon", "equalto", horizon) | list %}
{% if steps_in_horizon %}
| ID | Title | Quarter | Owner | Success gate |
|---|---|---|---|---|
{% for s in steps_in_horizon %}
| `{{ s.id }}` | {{ s.title }} | {{ s.quarter }} | {{ s.owner }} | {{ s.success_gate }} |
{% endfor %}
{% else %}
_No steps planned at this horizon._
{% endif %}

{% endfor %}

---

_Generated by phronesis. P7 ✓ ≤5 frameworks active; horizons aligned to {{ thesis.strategic_horizon }} thesis._
