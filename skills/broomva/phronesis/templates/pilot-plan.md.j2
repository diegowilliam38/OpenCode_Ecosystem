{# templates/pilot-plan.md.j2 — Pilot Plan (Deliverable 7 of 7).

PilotDesign with explicit success criteria, kill criterion, adoption metric
(L4), and baselines (L5). The Bision-failure-prevention deliverable.
#}
# Pilot Plan — `{{ pilot.use_case_id }}`

**Engagement:** `{{ tenant.tenant_slug }}`
**Date:** {{ generated_at }}
**Duration:** {{ pilot.duration_weeks }} weeks
**Cost estimate:** {{ pilot.cost_estimate | as_currency }}

## Hypothesis

> {{ pilot.hypothesis }}

**Null hypothesis:** {{ pilot.null_hypothesis }}

## Cohort

{{ pilot.cohort_definition }}

## Success criteria

{% for c in pilot.success_criteria %}
- {{ c }}
{% endfor %}

**Kill criterion.** {{ pilot.kill_criterion }}

## L4 — Adoption metric (Bision Failure 4)

| | |
|---|---|
| **Metric** | {{ pilot.adoption_metric.metric_name }} |
| **Target** | {{ pilot.adoption_metric.target_value }} |
| **Measurement** | {{ pilot.adoption_metric.measurement_method }} |
| **Owner** | {{ pilot.adoption_metric.owner }} |

## L5 — Baselines declared BEFORE pilot start (Bision Failure 5)

| Metric | Baseline | Window | Captured by | Date |
|---|---|---|---|---|
{% for b in pilot.baseline %}
{% if b.is_greenfield %}
| {{ b.metric_name }} | **[GREENFIELD]** zero-state declared | {{ b.baseline_window }} | {{ b.captured_by }} | {{ b.baseline_measurement_date.date() }} |
{% else %}
| {{ b.metric_name }} | {{ b.baseline_value }} | {{ b.baseline_window }} | {{ b.captured_by }} | {{ b.baseline_measurement_date.date() }} |
{% endif %}
{% endfor %}

{% set greenfield_count = pilot.baseline | selectattr("is_greenfield") | list | length %}
{% set incumbent_count = pilot.baseline | length - greenfield_count %}
{% if greenfield_count > 0 %}
> **Greenfield pilot.** {{ greenfield_count }} of {{ pilot.baseline | length }} baseline(s) declared zero-state — no incumbent metric exists. The L5 invariant becomes "declare zero-state explicitly" rather than "capture incumbent metric"; protects against the failure mode where a team SAYS they have a baseline but it's `0` because they didn't measure.
{% endif %}

## Learning objectives

{% for o in pilot.learning_objectives %}
- {{ o }}
{% endfor %}

## Risks

{% for r in pilot.risks %}
- *{{ r.severity }}/{{ r.confidence }}* — **{{ r.title }}**: {{ r.body }}
{% endfor %}

---

_Generated by phronesis. L4 ✓ adoption_metric required + non-technical owner; L5 ✓ {{ pilot.baseline | length }} baseline(s) declared before pilot start ({{ greenfield_count }} greenfield, {{ incumbent_count }} incumbent — no retroactive baselines); P3 ✓ all elements cite evidence._
