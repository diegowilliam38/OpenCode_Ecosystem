{# templates/maturity-report.md.j2 — Maturity Report (Deliverable 1 of 7).

Worked template for D.2-D.7. Each remaining deliverable copies this shape:
front-matter (tenant + frameworks + thesis), main sections (typed-primitive
list rendered as table + per-item detail), footer (P3/P7 invariants noted).

Strict undefined: every var referenced here MUST be in the context dict
or render() raises UndefinedError. #}
# Maturity Report — {{ tenant.name }}

**Engagement:** `{{ tenant.tenant_slug }}`
**Industry:** {{ tenant.industry }} ({{ tenant.region }})
**Date:** {{ generated_at }}
**Sponsor:** {{ tenant.sponsor }} ({{ tenant.sponsor_role }})
**Frameworks applied:** {{ frameworks_applied | join(", ") }}

## Strategic Thesis

> {{ thesis.economic_lever }}

| | |
|---|---|
| **Magnitude** | {{ thesis.magnitude_estimate | as_currency }} ({{ thesis.lever_kind }}) |
| **Horizon** | {{ thesis.strategic_horizon }} |
| **Owner** | {{ thesis.decision_rights_owner }} |
| **Measured in** | {{ thesis.measured_in }} |
| **Basis** | {{ thesis.magnitude_basis }} |

## Maturity Dimensions

| Dimension | Current | Target | Gap |
|---|---|---|---|
{% for d in dimensions %}
| {{ d.name }} | {{ "%.1f" | format(d.current_score.value) }} | {{ "%.1f" | format(d.target_score.value) }} | {{ d.gap_summary }} |
{% endfor %}

### Key actions per dimension

{% for d in dimensions %}
**{{ d.name }}** ({{ d.framework_ref }})

> {{ d.gap_summary }}

{% for action in d.key_actions %}
- {{ action }}
{% endfor %}

{% endfor %}

## Capability Heatmap

See companion deliverable: **Capability Heatmap** (`capability-heatmap.md`).

---

_Generated by phronesis. P3 ✓ all dimensions cite evidence; P7 ✓ ≤5 frameworks active._
